<Page
    x:Class="PocketTavern.UWP.Views.DebugLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:PocketTavern.UWP.Controls"
    Background="{StaticResource BackgroundDeepBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="{StaticResource BackgroundSurfaceBrush}" Padding="4,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Background="Transparent" BorderThickness="0"
                    Foreground="{StaticResource TextPrimaryBrush}" Click="OnBackClick"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="18" />
            </Button>
            <TextBlock Grid.Column="1" Text="Debug Log" Style="{StaticResource TitleTextStyle}"
                       VerticalAlignment="Center" Margin="4,0" />
            <Button Grid.Column="2" Background="Transparent" BorderThickness="0"
                    Foreground="{StaticResource AccentPrimaryBrush}"
                    Click="OnCopyClick" Padding="8,4"
                    ToolTipService.ToolTip="Copy all to clipboard">
                <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="18" />
            </Button>
            <Button Grid.Column="3" Background="Transparent" BorderThickness="0"
                    Foreground="{StaticResource TextSecondaryBrush}"
                    Click="OnClearClick" Padding="8,4"
                    ToolTipService.ToolTip="Clear log">
                <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="18" />
            </Button>
        </Grid>

        <!-- Log entries -->
        <ScrollViewer Grid.Row="1" x:Name="LogScroll"
                      VerticalScrollBarVisibility="Auto"
                      Background="{StaticResource BackgroundDeepBrush}">
            <controls:SpacedPanel x:Name="LogPanel" Margin="8,8,8,8" Spacing="2" />
        </ScrollViewer>

        <!-- Empty state -->
        <StackPanel Grid.Row="1" x:Name="EmptyState"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="Collapsed">
            <TextBlock Text="&#xEBE8;" FontFamily="Segoe MDL2 Assets" FontSize="40"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}" />
            <TextBlock Text="No log entries yet" FontSize="14"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,8,0,0" />
            <TextBlock Text="Send a message to see API traffic here"
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Auto-scroll toggle -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundSurfaceBrush}" Padding="16,6">
            <controls:SpacedPanel Orientation="Horizontal" Spacing="8">
                <ToggleSwitch x:Name="AutoScrollToggle" IsOn="True"
                              Margin="0,0,-8,0" VerticalAlignment="Center" />
                <TextBlock Text="Auto-scroll to bottom"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" VerticalAlignment="Center" />
            </controls:SpacedPanel>
        </Border>
    </Grid>
</Page>
